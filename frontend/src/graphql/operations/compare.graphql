query Files($rev: String!, $repo: String, $w: Boolean, $patch: String) {
  files(rev: $rev, repo: $repo, w: $w, patch: $patch) {
    path
    status
    additions
    deletions
    visibleLines
    visibleLinesSplit
  }
}
